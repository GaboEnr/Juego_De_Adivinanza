/**
let contador = 1;

while (contador < 4) {
  console.log('Ejecutando la iteración ' + contador);
  contador = contador + 1;
} /**
 */

let contador = 0;

while(contador <= 10){
  console.log('Ejecutando la interacion ' +contador)
  contador++; //este es la variable que da el inicio 
}


let contdor = 10;

while(contdor >= 0){
  console.log('Ejecutando la interacion' +contdor)
  contdor--; //este es la variable que da el inicio 
}


let numeroMax = prompt("ingrese un numero");

while(numeroMax >= 0){
  console.log(numeroMax);
  numeroMax--;
}


let numMax = prompt("ingrese un numero");

let cont = 0;

while(cont <= numMax){
  console.log(cont);
  cont++;
}

